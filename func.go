package main

import "fmt"

func PrintValue(){ // function name should start with a capital letter if we want to access it from another package(public). Otherwise, it will be private to the package
	fmt.Print(url)
}